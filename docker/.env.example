# Domain + TLS (Caddy / Let's Encrypt)
DOMAIN=example.com
EMAIL=you@example.com

# Postgres (optional overrides for docker-compose.prod.yml)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=link1die_db

